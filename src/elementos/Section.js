import styled from 'styled-components';

const Section = styled.section `
 text-align: center;
`
const TituloSeccionUltimos = styled.h2 `
     text-align:center;
`
export {Section,TituloSeccionUltimos };